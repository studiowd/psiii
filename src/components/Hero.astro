<section class="hero" id="inicio">
  <div class="container hero__inner">

    <div class="hero__content">

      <img
        src="/images/logo.svg"
        alt="EPPS"
        class="hero__mobile-logo reveal-scale"
      />

      <p class="hero__eyebrow reveal-fade delay-1">
        Escuela Psicoanalítica de Psicología Social
      </p>

      <h1 class="hero__title reveal delay-1">
        Formación y transmisión
        <span class="mobile-break"></span>
        <span class="desktop-space"> </span>

        del pensamiento psicosocial
        <span class="mobile-break"></span>
        <span class="desktop-space"> </span>

        y psicoanalítico
      </h1>

      <p class="hero__description reveal delay-2">
        Lectura rigurosa del psiquismo en lo individual, lo vincular, lo grupal,
        lo institucional y lo comunitario.
      </p>

      <a
        class="btn btn-blue hero__cta reveal delay-3"
        href="#formacion"
      >
        Planes de estudio
      </a>

    </div>

    <div class="hero__visual">
      <img
        src="/images/logo.svg"
        alt="EPPS"
        class="hero__visual-logo reveal-right delay-2"
      />
    </div>

  </div>
</section>

<style>
  .hero {
    position: relative;
    overflow: hidden;
    background: linear-gradient(105deg, #b80000 0%, var(--color-red) 70%, #df0c0c 100%);
    color: var(--color-white);
  }

  .hero__inner {
    min-height: clamp(530px, 120vw, 640px);
    display: grid;
    align-items: center;
  }

  .hero__content {
    position: relative;
    z-index: 2;
    width:  clamp(90vw, 100vw, 90vw);
    max-width: 700px;
    padding-block: clamp(42px, 10vw, 72px);
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    margin-inline: auto;
  }

  .hero__mobile-logo {
    display: block;
    width: clamp(118px, 34vw, 160px);
    height: auto;
    margin-bottom: clamp(24px, 7vw, 34px);
  }

  .hero__eyebrow {
    margin-bottom: clamp(10px, 4vw, 24px);
    color: var(--color-yellow);
    font-family: var(--font-heading);
    font-size: clamp(.35rem, 2.8vw, .70rem);
    line-height: 1.3;
    font-weight: 800;
    letter-spacing: .08em;
    text-transform: uppercase;
  }

  .hero__title {
   
    font-size: clamp(1.60rem, 2.8vw, 1.87rem);
    line-height: 1.4;
    font-weight: 800;
    letter-spacing: -.04em;
  }

  .mobile-break {
    display: block;
  }

  .desktop-space {
    display: none;
  }

  .hero__description {
    width:  clamp(70vw, 100vw, 70px);
    margin-top: clamp(22px, 5vw, 30px);
    font-size: clamp(1rem, 3.6vw, 1.08rem);
    line-height: 1.42;
    color: rgba(255, 255, 255, .96);
  }

  .hero__cta {
    margin-top: clamp(28px, 6vw, 36px);
    min-width: clamp(185px, 48vw, 220px);
  }

  .hero__visual {
    display: none;
  }

  /* =========================================================
   TABLET 768px - 1023px
   ========================================================= */

@media (min-width: 768px) and (max-width: 1023px) {

  .hero {
    min-height: 395px;
  }

  .hero__inner {
    min-height: 395px;

    display: grid;

    grid-template-columns:
      minmax(0, 1.25fr)
      minmax(240px, .75fr);

    gap: 30px;

    align-items: center;

    padding-block: 48px;
  }


  /* =========================================
     CONTENIDO IZQUIERDA
     ========================================= */

  .hero__content {
    width: 100%;
    max-width: 520px;

    margin: 0;
    padding: 0;

    display: flex;
    flex-direction: column;

    align-items: flex-start;

    text-align: left;
  }


  /* LOGO MOBILE OCULTO */

  .hero__mobile-logo {
    display: none;
  }


  /* EYEBROW */

  .hero__eyebrow {
    margin: 0 0 34px;

    font-size: .68rem;
    line-height: 1.2;

    letter-spacing: .06em;

    text-align: left;
  }


  /* =========================================
     TÍTULO
     ========================================= */

  .mobile-break {
    display: none;
  }

  .desktop-space {
    display: inline;
  }


  .hero__title {
    width: 100%;
    max-width: 430px;

    margin: 0;

    font-size: clamp(1.75rem, 3.5vw, 2rem);

    line-height: 1.08;

    letter-spacing: -.035em;

    text-align: left;
  }


  /* =========================================
     DESCRIPCIÓN
     ========================================= */

  .hero__description {
    width: 100%;
    max-width: 470px;

    margin-top: 12px;

    font-size: .82rem;

    line-height: 1.35;

    text-align: left;
  }


  /* =========================================
     CTA
     ========================================= */

  .hero__cta {
    margin-top: 28px;

    min-width: 0;

    align-self: flex-start;
  }


  /* =========================================
     VISUAL DERECHA
     ========================================= */

  .hero__visual {
    display: flex;

    width: 100%;
    height: 100%;

    align-items: flex-start;
    justify-content: flex-end;

    margin: 0;

    min-width: 0;
  }


  .hero__visual-logo {
    display: block;

    width: clamp(230px, 34vw, 300px);

    max-width: none;

    height: auto;
  }

}

  @media (min-width: 1024px) {
    .hero__inner {
      min-height: clamp(500px, 42vw, 620px);
      grid-template-columns: minmax(0, 1.2fr) minmax(320px, .8fr);
      gap: clamp(36px, 4vw, 70px);
    }

    .hero__content {
      max-width: 760px;
      margin: 0;
      padding-block: clamp(64px, 6vw, 88px);
      align-items: flex-start;
      text-align: left;
    }

    .hero__mobile-logo {
      display: none;
    }

    .hero__eyebrow {
      font-size: clamp(.68rem, .8vw, .84rem);
    }

    .mobile-break {
      display: none;
    }

    .desktop-space {
      display: inline;
    }

    .hero__title {
       max-width: 425px;
    font-size: clamp(1.60rem, 2.8vw, 1.90rem);
    line-height: 1.4;
    }

    .hero__description {
      max-width: 640px;
      font-size: clamp(.98rem, 1.2vw, 1.15rem);
    }

    .hero__visual {
      display: flex;
      align-items: center;
      justify-content: flex-end;
      min-width: 0;
    }

    .hero__visual-logo {
      width: clamp(320px, 30vw, 430px);
      max-width: none;
      height: auto;
    }
  }

  @media (min-width: 1280px) {
    .hero__inner {
      grid-template-columns: minmax(0, 1.2fr) 440px;
    }

    .hero__visual-logo {
      width: clamp(390px, 31vw, 440px);
    }
  }
</style>
