<section class="hero" id="inicio">
  <div class="container hero__inner">

    <!-- =====================================================
         CONTENIDO
         ===================================================== -->
    <div class="hero__content">

      <!-- LOGO MOBILE -->
      <img
        src="/images/logo.svg"
        alt="EPPS"
        class="hero__mobile-logo reveal-scale"
      />

      <!-- EYEBROW -->
      <p class="hero__eyebrow reveal-fade delay-1">
        Escuela Psicoanalítica de Psicología Social
      </p>

      <!-- =====================================================
           TÍTULO
           Un solo H1 semántico.
           Dos composiciones visuales.
           ===================================================== -->
      <h1
        class="hero__title reveal delay-1"
        aria-label="Formación y transmisión del pensamiento psicosocial y psicoanalítico"
      >

        <!-- MOBILE -->
        <span class="hero__title-mobile" aria-hidden="true">
          <span>Formación y transmisión</span>
          <span>del pensamiento psicosocial</span>
          <span>y psicoanalítico</span>
        </span>

        <!-- DESKTOP -->
        <span class="hero__title-desktop" aria-hidden="true">
          <span>Formación y transmisión</span>
          <span>del pensamiento</span>
          <span>psicosocial y psicoanalítico</span>
        </span>

      </h1>

      <!-- =====================================================
           DESCRIPCIÓN
           ===================================================== -->

      <p
        class="hero__description reveal delay-2"
        aria-label="Lectura rigurosa del psiquismo en lo individual, lo vincular, lo grupal, lo institucional y lo comunitario."
      >

        <!-- MOBILE -->
        <span class="hero__description-mobile" aria-hidden="true">
          <span>Lectura rigurosa del psiquismo en lo</span>
          <span>individual, lo vincular, lo grupal, lo</span>
          <span>institucional y lo comunitario.</span>
        </span>

        <!-- DESKTOP -->
        <span class="hero__description-desktop" aria-hidden="true">
          <span>
            Lectura rigurosa del psiquismo en lo individual, lo vincular,
          </span>

          <span>
            lo grupal, lo institucional y lo comunitario.
          </span>
        </span>

      </p>

      <!-- CTA -->
      <a
        class="btn btn-blue hero__cta reveal delay-3"
        href="#formacion"
      >
        Planes de estudio
      </a>

    </div>


    <!-- =====================================================
         LOGO DESKTOP
         ===================================================== -->
    <div class="hero__visual">

      <img
        src="/images/logo.svg"
        alt="EPPS"
        class="hero__visual-logo reveal-right delay-2"
      />

    </div>

  </div>
</section>


<style>

  /* =========================================================
     HERO
     ========================================================= */

  .hero {
    position: relative;
    overflow: hidden;

    background:
      linear-gradient(
        105deg,
        #b80000 0%,
        var(--color-red) 70%,
        #df0c0c 100%
      );

    color: var(--color-white);
  }


  .hero__inner {
    display: grid;
    align-items: center;
  }



  /* =========================================================
     MOBILE FIRST
     ========================================================= */

  .hero__inner {
    min-height: 350px;
  }


  .hero__content {
    position: relative;
    z-index: 2;

    width: 100%;
    max-width: 430px;

    margin-inline: auto;

    padding: 22px 8px 30px;

    display: flex;
    flex-direction: column;
    align-items: center;

    text-align: center;
  }



  /* =========================================================
     LOGO MOBILE
     ========================================================= */

  .hero__mobile-logo {
    display: block;

    width: 88px;
    height: auto;

    margin: 0 0 18px;
  }



  /* =========================================================
     EYEBROW
     ========================================================= */

  .hero__eyebrow {
    margin: 0 0 17px;

    color: var(--color-yellow);

    font-family: var(--font-heading);

    font-size: 0.54rem;
    line-height: 1.2;

    font-weight: 800;

    letter-spacing: 0.025em;

    text-transform: uppercase;

    white-space: nowrap;
  }



  /* =========================================================
     TÍTULO GENERAL
     ========================================================= */

  .hero__title {
    width: 100%;

    margin: 0;

    font-family: var(--font-heading);

    font-weight: 800;

    letter-spacing: -0.04em;

    color: var(--color-white);
  }



  /* =========================================================
     TÍTULO MOBILE
     ========================================================= */

  .hero__title-mobile {
    display: block;

    width: 100%;

    font-size: clamp(
      1.08rem,
      5.6vw,
      1.32rem
    );

    line-height: 1.16;
  }


  .hero__title-mobile > span {
    display: block;

    white-space: nowrap;
  }



  /* Desktop oculto en mobile */

  .hero__title-desktop {
    display: none;
  }



  /* =========================================================
     DESCRIPCIÓN
     ========================================================= */

  .hero__description {
    width: 100%;

    margin: 16px 0 0;

    color: rgba(255,255,255,.97);
  }


  .hero__description-mobile {
    display: block;

    font-size: clamp(
      0.7rem,
      3.25vw,
      0.82rem
    );

    line-height: 1.27;
  }


  .hero__description-mobile > span {
    display: block;

    white-space: nowrap;
  }


  .hero__description-desktop {
    display: none;
  }



  /* =========================================================
     CTA
     ========================================================= */

  .hero__cta {
    margin-top: 23px;

    min-width: 160px;

    padding: 10px 20px;

    align-self: center;

    font-size: 0.58rem;

    font-weight: 800;

    text-transform: uppercase;
  }



  /* =========================================================
     VISUAL DESKTOP
     ========================================================= */

  .hero__visual {
    display: none;
  }



  /* =========================================================
     MOBILE PEQUEÑO
     <= 389px
     ========================================================= */

  @media (max-width: 389px) {

    .hero__inner {
      min-height: 350px;
    }


    .hero__content {
      padding:
        18px
        5px
        26px;
    }


    .hero__mobile-logo {
      width: 78px;

      margin-bottom: 15px;
    }


    .hero__eyebrow {
      margin-bottom: 15px;

      font-size: 0.46rem;
    }


    .hero__title-mobile {
      font-size: 1.06rem;

      line-height: 1.17;
    }


    .hero__description {
      margin-top: 15px;
    }


    .hero__description-mobile {
      font-size: 0.69rem;

      line-height: 1.26;
    }


    .hero__cta {
      margin-top: 21px;

      min-width: 150px;
    }

  }



  /* =========================================================
     390px - 430px
     TU REFERENCIA MOBILE
     ========================================================= */

  @media (min-width: 390px) and (max-width: 430px) {

    .hero__inner {
      min-height: 390px;
    }


    .hero__content {
      max-width: 430px;

      padding:
        24px
        6px
        30px;
    }


    .hero__mobile-logo {
      width: 92px;

      margin-bottom: 18px;
    }


    .hero__eyebrow {
      margin-bottom: 17px;

      font-size: 0.55rem;
    }


    .hero__title-mobile {
      font-size: 1.30rem;

      line-height: 1.15;
    }


    .hero__description {
      margin-top: 17px;
    }


    .hero__description-mobile {
      font-size: 0.81rem;

      line-height: 1.27;
    }


    .hero__cta {
      margin-top: 23px;

      min-width: 160px;
    }

  }



  /* =========================================================
     431px - 767px
     ========================================================= */

  @media (min-width: 431px) and (max-width: 767px) {

    .hero__inner {
      min-height: 430px;
    }


    .hero__content {
      max-width: 540px;

      padding-block: 36px;
    }


    .hero__mobile-logo {
      width: 105px;

      margin-bottom: 20px;
    }


    .hero__eyebrow {
      font-size: 0.62rem;
    }


    .hero__title-mobile {
      font-size: 1.5rem;
    }


    .hero__description-mobile {
      font-size: 0.88rem;
    }


    .hero__cta {
      margin-top: 26px;

      min-width: 180px;
    }

  }



  /* =========================================================
     TABLET / DESKTOP
     768px+
     ========================================================= */

  @media (min-width: 768px) {

    .hero__inner {
      min-height: 430px;

      grid-template-columns:
        minmax(0, 1.35fr)
        minmax(260px, .65fr);

      gap: 36px;
    }


    .hero__content {
      width: 100%;

      max-width: 680px;

      margin: 0;

      padding: 50px 0;

      align-items: flex-start;

      text-align: left;
    }



    /* Logo mobile OFF */

    .hero__mobile-logo {
      display: none;
    }



    /* EYEBROW */

    .hero__eyebrow {
      margin-bottom: 28px;

      font-size: 0.70rem;

      text-align: left;

      white-space: normal;
    }



    /* =====================================================
       CAMBIO TITULO MOBILE -> DESKTOP
       ===================================================== */

    .hero__title-mobile {
      display: none;
    }


    .hero__title-desktop {
      display: block;

      width: 100%;

      font-size: clamp(
        2rem,
        3.4vw,
        2.45rem
      );

      line-height: 1.06;
    }


    .hero__title-desktop > span {
      display: block;

      white-space: nowrap;
    }



    /* =====================================================
       DESCRIPCIÓN DESKTOP
       ===================================================== */

    .hero__description-mobile {
      display: none;
    }


    .hero__description-desktop {
      display: block;

      margin-top: 0;

      font-size: 0.90rem;

      line-height: 1.35;
    }


    .hero__description-desktop > span {
      display: block;

      white-space: nowrap;
    }



    .hero__description {
      max-width: 620px;

      margin-top: 14px;

      text-align: left;
    }



    /* CTA */

    .hero__cta {
      margin-top: 28px;

      min-width: 200px;

      align-self: flex-start;

      font-size: 0.70rem;
    }



    /* LOGO DERECHA */

    .hero__visual {
      display: flex;

      width: 100%;

      min-width: 0;

      align-items: center;

      justify-content: flex-end;
    }


    .hero__visual-logo {
      display: block;

      width: clamp(
        250px,
        31vw,
        340px
      );

      max-width: none;

      height: auto;
    }

  }



  /* =========================================================
     1024px+
     ========================================================= */

  @media (min-width: 1024px) {

    .hero__inner {
      min-height: 470px;

      grid-template-columns:
        minmax(0, 1.3fr)
        minmax(340px, .7fr);

      gap: 46px;
    }


    .hero__content {
      max-width: 740px;

      padding-block: 58px;
    }


    .hero__eyebrow {
      margin-bottom: 32px;

      font-size: 0.78rem;
    }


    .hero__title-desktop {
      font-size: clamp(
        2.15rem,
        2.75vw,
        2.65rem
      );

      line-height: 1.04;
    }


    .hero__description {
      margin-top: 16px;
    }


    .hero__description-desktop {
      font-size: 1rem;

      line-height: 1.38;
    }


    .hero__cta {
      margin-top: 30px;

      min-width: 220px;

      font-size: 0.72rem;
    }


    .hero__visual-logo {
      width: clamp(
        320px,
        32vw,
        390px
      );
    }

  }



  /* =========================================================
     1280px+
     ========================================================= */

  @media (min-width: 1280px) {

    .hero__inner {
      min-height: 440px;

      grid-template-columns:
        minmax(0, 1.35fr)
        420px;

      gap: 40px;
    }


    .hero__content {
      max-width: 760px;

      padding-block: 50px;
    }


    .hero__eyebrow {
      margin-bottom: 30px;

      font-size: 0.82rem;
    }


    .hero__title-desktop {
      font-size: 2.55rem;

      line-height: 1.03;
    }


    .hero__description-desktop {
      font-size: 1rem;

      line-height: 1.4;
    }


    .hero__visual-logo {
      width: 390px;
    }

  }



  /* =========================================================
     1440px+
     TU REFERENCIA DESKTOP
     ========================================================= */

  @media (min-width: 1440px) {

    .hero__inner {
      min-height: 435px;

      grid-template-columns:
        minmax(0, 1fr)
        460px;

      gap: 44px;
    }


    .hero__content {
      max-width: 780px;

      padding-block: 48px;
    }


    .hero__eyebrow {
      margin-bottom: 30px;

      font-size: 0.84rem;
    }


    .hero__title-desktop {
      font-size: 2.65rem;

      line-height: 1.02;
    }


    .hero__description {
      margin-top: 16px;
    }


    .hero__description-desktop {
      font-size: 1.02rem;

      line-height: 1.38;
    }


    .hero__cta {
      margin-top: 30px;
    }


    .hero__visual-logo {
      width: 420px;
    }

  }

</style>