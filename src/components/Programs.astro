<section id="formacion" class="programs section">
  <div class="container programs__inner">

    <header class="programs__heading">
      <p class="eyebrow reveal">Planes de estudio</p>

      <h2 class="reveal delay-1">
        Propuesta académica
      </h2>

      <p class="programs__lead reveal delay-2">
        Programas certificados por la Cámara Argentina de Formación Profesional,
        con posibilidad de revalidación internacional a través de la OIEP.
      </p>
    </header>


    <div class="programs-grid">

      <!-- CARD 1 -->
      <article class="program-card reveal-left">
        <div>
          <h3>Psicología Social Psicoanalítica</h3>

          <p>
            Formación orientada a la intervención en vínculos, grupos,
            instituciones y comunidades, desde una perspectiva psicoanalítica.
          </p>
        </div>

        <a class="program-card__link" href="#contacto">
          Plan de estudios
        </a>
      </article>


      <!-- IMAGEN -->
      <div class="program-image reveal-scale delay-1">
        <img
          src="/images/maestros.png"
          alt="Referentes del pensamiento psicoanalítico"
          loading="lazy"
        />
      </div>


      <!-- CARD 2 -->
      <article class="program-card reveal-right delay-2">
        <div>
          <h3>Seminario de Psicoanálisis</h3>

          <p>
            Espacio de estudio y transmisión del pensamiento psicoanalítico
            freudiano y su aplicación a los ámbitos propios de la Psicología
            Social pichoniana. “Retorno a Freud”: el psicoanálisis entendido
            desde Lacan, quien lee a Freud desde nuevos paradigmas.
          </p>
        </div>

        <a class="program-card__link" href="#contacto">
          Plan de estudios
        </a>
      </article>

    </div>


    <div class="programs__classes-cta reveal delay-3">
      <a href="#clases" class="programs__classes-button">
        Ver nuestras clases
      </a>
    </div>

  </div>
</section>


<style>

  /* ==================================================
     SECTION
  ================================================== */

  .programs {
    background: var(--color-navy-secondary);
    color: var(--color-white);

    padding-block: clamp(62px, 7vw, 112px);

    overflow: hidden;
  }


  .programs__inner {
    width: 100%;
    max-width: 1280px;
  }


  /* ==================================================
     HEADING
  ================================================== */

  .programs__heading {
    width: 100%;
    max-width: 820px;

    margin:
      0
      auto
      clamp(38px, 5vw, 68px);

    text-align: center;
  }


  .programs__heading .eyebrow {
    margin-bottom: clamp(12px, 1.5vw, 20px);

    color: var(--color-red);
  }


  .programs__heading h2 {
    margin: 0;

    color: var(--color-white);

    font-family: var(--font-heading);

    font-size: clamp(2.4rem, 4.2vw, 3.4rem);

    line-height: 0.98;

    letter-spacing: -0.04em;

    font-weight: 800;

    text-transform: uppercase;
  }


  .programs__lead {
    width: 100%;
    max-width: 760px;

    margin:
      clamp(18px, 2vw, 26px)
      auto
      0;

    color: rgba(255, 255, 255, 0.96);

    font-size: clamp(0.88rem, 1.15vw, 1.05rem);

    line-height: 1.48;
  }


  /* ==================================================
     GRID
     MOBILE FIRST
  ================================================== */

  .programs-grid {
    display: grid;

    grid-template-columns: 1fr;

    width: 100%;
    max-width: 354px;

    margin-inline: auto;

    gap: clamp(28px, 8vw, 38px);
  }


  /* ==================================================
     CARD
  ================================================== */

  .program-card {
    width: 100%;
    min-width: 0;

    padding:
      clamp(28px, 3vw, 46px)
      clamp(22px, 2.7vw, 34px)
      clamp(26px, 2.8vw, 36px);

    background: var(--color-white);

    color: var(--color-navy);

    border-radius:
      clamp(48px, 6vw, 72px)
      0
      clamp(48px, 6vw, 72px)
      0;

    display: flex;

    flex-direction: column;

    justify-content: space-between;

    overflow: hidden;
  }


  .program-card h3 {
    margin: 0;

    color: var(--color-navy);

    font-family: var(--font-heading);

    font-size: clamp(1.15rem, 2.15vw, 1.7rem);

    font-weight: 800;

    line-height: 1.08;

    letter-spacing: -0.035em;

    text-transform: uppercase;
  }


  .program-card p {
    margin:
      clamp(14px, 2vw, 24px)
      0
      0;

    color: var(--color-navy);

    font-size: clamp(0.88rem, 1.1vw, 0.98rem);

    line-height: 1.48;
  }


  /* ==================================================
     CARD BUTTON
  ================================================== */

  .program-card__link {
    align-self: flex-start;

    margin-top: clamp(26px, 3vw, 34px);

    padding:
      clamp(10px, 1vw, 12px)
      clamp(14px, 1.4vw, 18px);

    background: var(--color-red);

    color: var(--color-white);

    font-family: var(--font-heading);

    font-size: clamp(0.58rem, 0.72vw, 0.7rem);

    line-height: 1;

    letter-spacing: 0.04em;

    font-weight: 800;

    text-transform: uppercase;

    box-shadow:
      0 5px 12px rgba(0, 0, 0, 0.15);

    transition:
      transform 0.2s ease,
      background-color 0.2s ease;
  }


  .program-card__link:hover {
    transform: translateY(-2px);

    background: var(--color-red-hover);
  }


  /* ==================================================
     IMAGE
  ================================================== */

  .program-image {
    width: 100%;

    overflow: hidden;

    border-radius:
      clamp(48px, 6vw, 72px)
      0
      clamp(48px, 6vw, 72px)
      0;
  }


  .program-image img {
    display: block;

    width: 100%;
    height: 100%;

    object-fit: cover;

    object-position: center;

    margin: 0;
    padding: 0;
  }


  /* ==================================================
     CLASSES CTA
  ================================================== */

  .programs__classes-cta {
    display: flex;

    justify-content: center;

    margin-top: clamp(30px, 4vw, 52px);
  }


  .programs__classes-button {
    display: inline-flex;

    align-items: center;

    justify-content: center;

    min-height: clamp(38px, 4vw, 46px);

    padding:
      10px
      clamp(18px, 2vw, 24px);

    border-radius: 999px;

    background: var(--color-red);

    color: var(--color-white);

    font-family: var(--font-heading);

    font-size: clamp(0.58rem, 0.8vw, 0.72rem);

    font-weight: 800;

    letter-spacing: 0.04em;

    text-transform: uppercase;

    transition:
      transform 0.2s ease,
      background-color 0.2s ease;
  }


  .programs__classes-button:hover {
    transform: translateY(-2px);

    background: var(--color-red-hover);
  }


  /* ==================================================
     MOBILE
     320px — 767px
  ================================================== */

  @media (min-width: 320px) and (max-width: 767px) {

    .programs-grid {
      width: 100%;

      max-width: 354px;

      grid-template-columns: 1fr;

      gap: clamp(28px, 8vw, 38px);
    }


    .program-card,
    .program-image {
      width: 100%;

      max-width: 354px;

      margin-inline: auto;
    }


    .program-card {
      min-height: auto;
    }


    .program-image {
      height: clamp(300px, 90vw, 354px);
    }

  }


  /* ==================================================
     TABLET
     768px — 1023px
  ================================================== */

  @media (min-width: 768px) and (max-width: 1023px) {

    .programs-grid {
      width: 100%;

      max-width: none;

      grid-template-columns:
        repeat(3, minmax(0, 1fr));

      gap: clamp(16px, 2vw, 24px);

      align-items: stretch;
    }


    .program-card,
    .program-image {
      width: 100%;

      min-width: 0;

      height: clamp(330px, 42vw, 390px);
    }


    .program-card {
      padding:
        clamp(22px, 2.8vw, 30px)
        clamp(16px, 2vw, 22px)
        clamp(22px, 2.5vw, 28px);
    }


    .program-card h3 {
      font-size: clamp(1rem, 2.1vw, 1.3rem);
    }


    .program-card p {
      font-size: clamp(0.72rem, 1.4vw, 0.86rem);
    }


    .program-image img {
      width: 100%;

      height: 100%;

      object-fit: cover;

      object-position: center;
    }

  }


  /* ==================================================
     DESKTOP
     1024px — 1279px
  ================================================== */

  @media (min-width: 1024px) and (max-width: 1279px) {

    .programs-grid {
      width: 100%;

      max-width: none;

      grid-template-columns:
        repeat(3, minmax(0, 1fr));

      gap: clamp(24px, 2.5vw, 30px);

      align-items: stretch;
    }


    .program-card,
    .program-image {
      width: 100%;

      min-width: 0;

      height: clamp(390px, 34vw, 420px);
    }


    .program-card {
      padding:
        clamp(30px, 3vw, 40px)
        clamp(24px, 2.5vw, 30px)
        clamp(28px, 2.8vw, 34px);
    }


    .program-card h3 {
      font-size: clamp(1.45rem, 2vw, 1.7rem);
    }


    .program-card p {
      font-size: clamp(0.85rem, 1vw, 0.98rem);
    }


    .program-image img {
      width: 100%;

      height: 100%;

      object-fit: cover;

      object-position: center;
    }

  }


  /* ==================================================
     LARGE DESKTOP
     1280px+
  ================================================== */

  @media (min-width: 1280px) {

    .programs__inner {
      max-width: 1410px;
    }


    .programs-grid {
      width: 100%;

      max-width: none;

      grid-template-columns:
        repeat(3, minmax(0, 1fr));

      gap: 34px;

      align-items: stretch;
    }


    .program-card,
    .program-image {
      width: 100%;

      min-width: 0;

      height: 410px;
    }


    .program-card {
      padding:
        46px
        34px
        34px;
    }


    .program-card h3 {
      font-size: 1.9rem;
    }


    .program-card p {
      font-size: 0.98rem;
    }


    .program-image img {
      width: 100%;

      height: 100%;

      object-fit: cover;

      object-position: center;
    }

  }

</style>