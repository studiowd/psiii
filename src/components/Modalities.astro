<section class="epps-modalities" id="modalidades">
  <div class="container epps-modalities__inner">

    <div class="epps-modalities__intro">
      <h2 class="reveal">
        Cursá desde cualquier
        lugar del mundo
      </h2>

      <p class="reveal delay-1">
        Mantenemos el mismo nivel académico
        en nuestras tres modalidades:
      </p>
    </div>


    <div class="epps-modalities__grid">

      <!-- 1 -->
      <article class="epps-modality reveal-scale">

        <div class="epps-modality__icon">
          <i class="fa-regular fa-calendar-days" aria-hidden="true"></i>
        </div>

        <h3>
          Cursada semanal<br />
          por Zoom
        </h3>

      </article>


      <!-- 2 -->
      <article class="epps-modality reveal-scale delay-1">

        <div class="epps-modality__icon">
          <i class="fa-regular fa-calendar" aria-hidden="true"></i>

          <i
            class="fa-regular fa-clock modality-clock"
            aria-hidden="true"
          ></i>
        </div>

        <h3>
          Intensivo mensual<br />
          por Zoom
        </h3>

      </article>


      <!-- 3 -->
      <article class="epps-modality reveal-scale delay-2">

        <div class="epps-modality__icon">
          <i class="fa-regular fa-calendar" aria-hidden="true"></i>
          <span class="modality-seven">7</span>
        </div>

        <h3>
          A distancia<br />
          personalizado
        </h3>

      </article>

    </div>

  </div>
</section>

<style>

  /* =====================================================
     SECTION
  ===================================================== */

  .epps-modalities {
    width: 100%;

    background: #ffffff;

    padding:
      clamp(46px, 4.5vw, 72px)
      0;
  }


  .epps-modalities__inner {
    width: 100%;

    display: grid;

    align-items: center;
  }


  /* =====================================================
     INTRO
  ===================================================== */

  .epps-modalities__intro {
    margin: 0;
  }


  .epps-modalities__intro h2 {
    margin: 0;

    font-family: var(--font-heading);

    font-size: clamp(1.4rem, 3vw, 1.6rem);

    font-weight: 800;

    line-height: 1.02;

    letter-spacing: -0.035em;

    color: #CF0000;

    text-transform: none;
  }


  .epps-modalities__intro p {
    margin:
      clamp(18px, 1.6vw, 24px)
      0
      0;

    font-family: var(--font-body);

    font-size: clamp(17px, 1.3vw, 20px);

    font-weight: 400;

    line-height: 1.55;

    color: #141838;
  }


  /* =====================================================
     MODALITIES GRID
  ===================================================== */

  .epps-modalities__grid {
    display: grid;

    width: 100%;

    align-items: start;
  }


  /* =====================================================
     INDIVIDUAL MODALITY
     Fuerzo reset para evitar estilos viejos
  ===================================================== */

  .epps-modality {
    width: 100%;

    margin: 0 !important;

    padding: 0 !important;

    min-height: 0 !important;

    border: 0 !important;

    border-radius: 0 !important;

    box-shadow: none !important;

    background: transparent !important;

    display: flex;

    flex-direction: column;

    align-items: center;

    justify-content: flex-start;

    text-align: center;
  }


  .epps-modality h3 {
    margin:
      clamp(14px, 1.2vw, 18px)
      0
      0;

    padding: 0;

    font-family: var(--font-heading);

    font-size: clamp(14px, 1vw, 16px);

    font-weight: 800;

    line-height: 1.18;

    letter-spacing: -0.02em;

    color: #141838;
  }


  /* =====================================================
     ICON
  ===================================================== */

  .epps-modality__icon {
  position: relative;

  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 58px;
  height: 58px;
  margin-bottom: 0px;
  color: #cf0000;
  font-size: 3.25rem;
  line-height: 1;
  transition: transform 0.3s ease;
}


/* RELOJ DENTRO DEL CALENDARIO */

.modality-clock {
  position: absolute;
  left: 50%;
  top: 57%;
  transform: translate(-50%, -50%);
  padding: 2px;
  background: white;
  font-size: 1.05rem;
}


/* NÚMERO 7 */

.modality-seven {
  position: absolute;

  left: 50%;
  top: 57%;

  transform: translate(-50%, -50%);

  font-family: var(--font-heading);
  font-size: 0.9rem;
  line-height: 1;
  font-weight: 400;

  color: #cf0000;
}


/* HOVER */

.epps-modality:hover .epps-modality__icon {
  transform: translateY(-4px);
}

  /* =====================================================
     MOBILE
     320px — 767px
  ===================================================== */

  @media (min-width: 320px) and (max-width: 767px) {

    .epps-modalities {
      padding:
        42px
        0
        68px;
    }


    .epps-modalities__inner {
      grid-template-columns: 1fr;

      gap: 58px;
    }


    .epps-modalities__intro {
      width: 100%;

      max-width: 360px;

      margin-inline: auto;

      text-align: center;
    }


    /* En la referencia mobile no aparece el título rojo */

    .epps-modalities__intro h2 {
      display: block;
      padding-bottom:10px;
     font-size: clamp(28px, 2vw, 34px);
    }


    .epps-modalities__intro p {
      margin: 0 auto;

      max-width: 350px;

      font-size: clamp(17px, 5vw, 16spx);

      line-height: 1.5;

      text-align: center;
    }


    .epps-modalities__grid {
      grid-template-columns: 1fr;

      gap: 40px;
    }


    .epps-modality {
      background: transparent !important;
    }


    .epps-modality__icon {
      width: 58px;

      height: 58px;
    }


    .epps-modality h3 {
      margin-top: 0px;

      font-size: 15px;

      line-height: 1.35;
    }

  }


  /* =====================================================
   TABLET
   768px — 1023px
===================================================== */

@media (min-width: 768px) and (max-width: 1023px) {

  .epps-modalities__inner {
    width: calc(100% - 64px);
    max-width: 900px;

    margin-inline: auto;

    grid-template-columns:
      minmax(200px, 0.85fr)
      minmax(0, 2fr);

    gap: clamp(30px, 4vw, 42px);

    align-items: center;
  }

  .epps-modalities__intro {
    width: 100%;
    max-width: 250px;

    margin: 0;

    text-align: left;
  }

  .epps-modalities__intro h2 {
    font-size: clamp(24px, 3vw, 30px);
  }

  .epps-modalities__intro p {
    font-size: clamp(14px, 1.8vw, 18px);
  }

  .epps-modalities__grid {
    width: 100%;
    max-width: 580px;

    margin-inline: auto;

    grid-template-columns: repeat(3, minmax(0, 1fr));

    gap: clamp(18px, 3vw, 30px);

    align-items: start;
  }

}


  /* =====================================================
     DESKTOP
     1024+
  ===================================================== */

  @media (min-width: 1024px) {

    .epps-modalities {
      padding:
        clamp(58px, 5vw, 72px)
        0;
        margin-inline:auto;
         max-width: 1410px;
    }


    .epps-modalities__inner {

      /*
        referencia:
        intro + 3 modalidades
      */

      grid-template-columns:
        minmax(330px, 1.05fr)
        minmax(0, 2.95fr);

      gap:
        clamp(60px, 6vw, 100px);

      align-items: center;
    }


    .epps-modalities__intro {
      max-width: 340px;
      padding-top:15px;  
      text-align: left;
    }


    .epps-modalities__grid {
      grid-template-columns:
        repeat(3, minmax(0, 1fr));

      gap:
        clamp(70px, 8vw, 150px);

      align-items: start;
    }


    .epps-modality__icon {
      width: clamp(50px, 3.5vw, 58px);

      height: clamp(50px, 3.5vw, 58px);
    }


    .epps-modality h3 {
      margin-top: 16px;

      font-size: clamp(14px, 1vw, 16px);
    }

  }


  /* =====================================================
     1280+
  ===================================================== */

  @media (min-width: 1280px) {







    .epps-modalities__inner {
      grid-template-columns:
        400px
        minmax(0, 1fr);

      gap: clamp(80px, 7vw, 120px);
    }


    .epps-modalities__grid {
      gap: clamp(100px, 5vw, 160px);
    }

  }

</style>