<section class="epps-modalities" id="modalidades">
  <div class="container epps-modalities__inner">

    <div class="epps-modalities__intro">
      <h2 class="reveal">
        Cursá desde cualquier
        lugar del mundo
      </h2>

      <p class="reveal delay-1">
        Mantenemos el mismo nivel académico
        en nuestras tres modalidades:
      </p>
    </div>


    <div class="epps-modalities__grid">

  <!-- 1 -->
  <article class="epps-modality reveal-scale">

    <div class="epps-modality__icon">
      <img
        src="/images/semanal.png"
        alt=""
        aria-hidden="true"
        loading="lazy"
      />
    </div>

    <h3>
      Cursada semanal<br />
      por Zoom
    </h3>

  </article>


  <!-- 2 -->
  <article class="epps-modality reveal-scale delay-1">

    <div class="epps-modality__icon">
      <img
        src="/images/intensivo.png"
        alt=""
        aria-hidden="true"
        loading="lazy"
      />
    </div>

    <h3>
      Intensivo mensual<br />
      por Zoom
    </h3>

  </article>


  <!-- 3 -->
  <article class="epps-modality reveal-scale delay-2">

    <div class="epps-modality__icon">
      <img
        src="/images/distancia.png"
        alt=""
        aria-hidden="true"
        loading="lazy"
      />
    </div>

    <h3>
      A distancia<br />
      personalizado
    </h3>

  </article>

</div>

  </div>
</section>

<style>

  /* =====================================================
     SECTION
  ===================================================== */

.epps-modalities {
  width: 100%;
  background: var(--color-white);

  padding:
    clamp(46px, 4.5vw, 72px)
    0;
}


.epps-modalities__inner {
  width: 100%;

  display: grid;
  align-items: center;
}


/* =====================================================
   INTRO
===================================================== */

.epps-modalities__intro {
  margin: 0;
}


.epps-modalities__intro h2 {
  margin: 0;

  font-family: var(--font-heading);

  font-size: clamp(1.4rem, 3vw, 1.6rem);

  font-weight: 800;

  line-height: 1.02;

  letter-spacing: -0.035em;

  color: var(--color-red);

  text-transform: none;
}


.epps-modalities__intro p {
  margin:
    clamp(18px, 1.6vw, 24px)
    0
    0;

  font-family: var(--font-body);

  font-size: clamp(17px, 1.3vw, 20px);

  font-weight: 400;

  line-height: 1.55;

  color: var(--color-navy);
}


/* =====================================================
   MODALITIES GRID
===================================================== */

.epps-modalities__grid {
  display: grid;

  width: 100%;

  align-items: start;
}


/* =====================================================
   INDIVIDUAL MODALITY
===================================================== */

.epps-modality {
  width: 100%;

  margin: 0 !important;
  padding: 0 !important;

  min-height: 0 !important;

  border: 0 !important;
  border-radius: 0 !important;

  box-shadow: none !important;

  background: transparent !important;

  display: flex;

  flex-direction: column;

  align-items: center;
  justify-content: flex-start;

  text-align: center;
}


.epps-modality h3 {
  margin:
    clamp(14px, 1.2vw, 18px)
    0
    0;

  padding: 0;

  font-family: var(--font-heading);

  font-size: clamp(14px, 1vw, 16px);

  font-weight: 800;

  line-height: 1.18;

  letter-spacing: -0.02em;

  color: var(--color-navy);
}


/* =====================================================
   ICON / PNG
===================================================== */

.epps-modality__icon {
  width: clamp(58px, 5vw, 74px);
  height: clamp(58px, 5vw, 74px);

  display: flex;

  align-items: center;
  justify-content: center;

  margin-bottom: 0;

  transition:
    transform var(--transition-normal);
}


.epps-modality__icon img {
  display: block;

  width: 100%;
  height: 100%;

  object-fit: contain;
}


/* =====================================================
   HOVER
===================================================== */

@media (hover: hover) {

  .epps-modality:hover .epps-modality__icon {
    transform: translateY(-4px);
  }

}


/* =====================================================
   MOBILE
   320px — 767px
===================================================== */

@media (min-width: 320px) and (max-width: 767px) {

  .epps-modalities {
    padding:
      42px
      0
      68px;
  }


  .epps-modalities__inner {
    grid-template-columns: 1fr;

    gap: 58px;
  }


  .epps-modalities__intro {
    width: 100%;

    max-width: 360px;

    margin-inline: auto;

    text-align: center;
  }


  .epps-modalities__intro h2 {
    display: block;

    padding-bottom: 10px;

    font-size: clamp(28px, 8vw, 34px);
  }


  .epps-modalities__intro p {
    margin: 0 auto;

    max-width: 350px;

    font-size: clamp(16px, 4.3vw, 17px);

    line-height: 1.5;

    text-align: center;
  }


  .epps-modalities__grid {
    grid-template-columns: 1fr;

    gap: 40px;
  }


  .epps-modality {
    background: transparent !important;
  }


  .epps-modality__icon {
    width: 64px;
    height: 64px;
  }


  .epps-modality h3 {
    margin-top: 10px;

    font-size: 15px;

    line-height: 1.35;
  }

}


/* =====================================================
   TABLET
   768px — 1023px
===================================================== */

@media (min-width: 768px) and (max-width: 1023px) {

  .epps-modalities__inner {
    width: calc(100% - 64px);

    max-width: 900px;

    margin-inline: auto;

    grid-template-columns:
      minmax(200px, 0.85fr)
      minmax(0, 2fr);

    gap: clamp(30px, 4vw, 42px);

    align-items: center;
  }


  .epps-modalities__intro {
    width: 100%;

    max-width: 250px;

    margin: 0;

    text-align: left;
  }


  .epps-modalities__intro h2 {
    font-size: clamp(24px, 3vw, 30px);
  }


  .epps-modalities__intro p {
    font-size: clamp(14px, 1.8vw, 18px);
  }


  .epps-modalities__grid {
    width: 100%;

    max-width: 580px;

    margin-inline: auto;

    grid-template-columns:
      repeat(3, minmax(0, 1fr));

    gap: clamp(18px, 3vw, 30px);

    align-items: start;
  }


  .epps-modality__icon {
    width: 60px;
    height: 60px;
  }

}


/* =====================================================
   DESKTOP
   1024+
===================================================== */

@media (min-width: 1024px) {

  .epps-modalities {
    padding:
      clamp(58px, 5vw, 72px)
      0;

    margin-inline: auto;

    max-width: 1410px;
  }


  .epps-modalities__inner {
    grid-template-columns:
      minmax(330px, 1.05fr)
      minmax(0, 2.95fr);

    gap:
      clamp(60px, 6vw, 100px);

    align-items: center;
  }


  .epps-modalities__intro {
    max-width: 340px;

    padding-top: 15px;

    text-align: left;
  }


  .epps-modalities__grid {
    grid-template-columns:
      repeat(3, minmax(0, 1fr));

    gap:
      clamp(70px, 8vw, 150px);

    align-items: start;
  }


  .epps-modality__icon {
    width: clamp(60px, 4vw, 72px);
    height: clamp(60px, 4vw, 72px);
  }


  .epps-modality h3 {
    margin-top: 16px;

    font-size: clamp(14px, 1vw, 16px);
  }

}


/* =====================================================
   1280+
===================================================== */

@media (min-width: 1280px) {

  .epps-modalities__inner {
    grid-template-columns:
      400px
      minmax(0, 1fr);

    gap: clamp(80px, 7vw, 120px);
  }


  .epps-modalities__grid {
    gap: clamp(100px, 5vw, 160px);
  }

}
  }

</style>